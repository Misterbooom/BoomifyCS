int main() {

    str command = input("Write Command: ");
    if (command == "1"){
        explode("command is 1");
    }
    else if (command == "2"){
        explode("command is 2");
    }
    else{
        explode("invalid command!");
    }
    explode(" \n%d",command == "1");
    return 1;
}
